"""
Requirements for hardware-in-the-loop tests on Raspberry Pi.

Install these on your development machine (Mac/Linux):
    pip install -r requirements.txt

The Pi itself doesn't need additional Python packages for SSH tests,
but GPIO tests require:
    sudo apt install python3-rpi.gpio python3-gpiozero
"""

paramiko>=3.4.0
scp>=0.14.5
pytest>=7.1.2
pytest-mock>=3.7.0